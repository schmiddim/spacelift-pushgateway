name: Build and Push Docker Image
on:
  push:
    branches:
      - master
  workflow_dispatch:
    inputs:
      version:
        description: 'Semver tag to release (e.g. v0.0.5). Leave empty to auto-increment patch.'
        required: false
        default: ''

jobs:
  build_and_test:
    name: Build & Test the Application
    runs-on: ubuntu-latest
    outputs:
      commit_id: ${{ steps.meta.outputs.commit_id }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.0'
      - name: Cache Go modules
        uses: actions/cache@v6
        with:
          path: ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-
      - name: Tidy Go modules
        run: |
          go mod tidy
          if [[ -n $(git status --porcelain) ]]; then
            git config --global user.name "github-actions[bot]"
            git config --global user.email "github-actions[bot]@users.noreply.github.com"
            git add go.mod go.sum
            git commit -m "chore: run go mod tidy"
            git push
          else
            echo "No changes detected"
          fi
      - name: Build
        run: go build -v -o exporter .
      - name: Test
        run: go test -v -coverprofile cover.out ./...
      - name: Upload coverage reports to Codecov
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Login to DockerHub
        uses: docker/login-action@v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Set image metadata
        id: meta
        run: echo "commit_id=${GITHUB_SHA}" >> $GITHUB_OUTPUT
      - name: Build and push (latest + commit sha)
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            schmiddim/spacelift-pushgateway:latest
            schmiddim/spacelift-pushgateway:${{ steps.meta.outputs.commit_id }}
          platforms: linux/amd64, linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    name: Release (Semver)
    needs: build_and_test
    runs-on: ubuntu-latest
    if: |
      github.event_name == 'workflow_dispatch' ||
      contains(github.event.head_commit.author.name, 'dependabot') ||
      contains(github.event.head_commit.message, 'Bump ') ||
      contains(github.event.head_commit.message, 'bump ')
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Compute next patch version
        id: version
        run: |
          git fetch --tags
          if [[ -n "${{ github.event.inputs.version }}" ]]; then
            NEW_TAG="${{ github.event.inputs.version }}"
            [[ "$NEW_TAG" != v* ]] && NEW_TAG="v${NEW_TAG}"
          else
            LAST_TAG=$(git tag --list 'v[0-9]*.[0-9]*.[0-9]*' --sort=-version:refname | head -n1)
            if [[ -z "$LAST_TAG" ]]; then
              LAST_TAG="v0.0.0"
            fi
            MAJOR=$(echo "$LAST_TAG" | sed 's/v//' | cut -d. -f1)
            MINOR=$(echo "$LAST_TAG" | sed 's/v//' | cut -d. -f2)
            PATCH=$(echo "$LAST_TAG" | sed 's/v//' | cut -d. -f3)
            NEW_TAG="v${MAJOR}.${MINOR}.$((PATCH + 1))"
          fi
          echo "new_tag=$NEW_TAG" >> $GITHUB_OUTPUT
      - name: Create and push git tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag ${{ steps.version.outputs.new_tag }}
          git push origin ${{ steps.version.outputs.new_tag }}
      - name: Login to DockerHub
        uses: docker/login-action@v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Re-tag existing image with Semver (no rebuild)
        run: |
          docker buildx imagetools create \
            --tag schmiddim/spacelift-pushgateway:${{ steps.version.outputs.new_tag }} \
            schmiddim/spacelift-pushgateway:${{ needs.build_and_test.outputs.commit_id }}
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ steps.version.outputs.new_tag }} \
            --title "${{ steps.version.outputs.new_tag }}" \
            --notes "## Docker Image

          \`\`\`
          docker pull schmiddim/spacelift-pushgateway:${{ steps.version.outputs.new_tag }}
          \`\`\`

          ### Helm values.yaml
          \`\`\`yaml
          image:
            repository: schmiddim/spacelift-pushgateway
            tag: \"${{ steps.version.outputs.new_tag }}\"
          \`\`\`"
      - name: Job Summary
        run: |
          TAG=${{ steps.version.outputs.new_tag }}
          IMAGE="schmiddim/spacelift-pushgateway"
          cat >> $GITHUB_STEP_SUMMARY << EOF
          ## Release ${TAG}

          ### Docker Pull
          \`\`\`
          docker pull ${IMAGE}:${TAG}
          \`\`\`

          ### Helm values.yaml
          \`\`\`yaml
          image:
            repository: ${IMAGE}
            tag: "${TAG}"
          \`\`\`

          ### Links
          - [DockerHub](https://hub.docker.com/r/${IMAGE}/tags?name=${TAG})
          - [GitHub Release](https://github.com/${{ github.repository }}/releases/tag/${TAG})
          EOF
